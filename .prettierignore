dist
dev-dist
coverage
node_modules
.agents
.claude/worktrees
package-lock.json
workouts_corrected.csv
# Vendored impeccable design skills — third-party, not our style domain.
.claude/skills/impeccable
# The v3 "Plate" design handoff. These are the canonical spec as Claude Design
# delivered them, and `npm run format` rewrites all five — which would edit the
# thing the app is being measured against. They are read, never authored here.
docs/design/v3-plate
# The v5 bundle is a NORMATIVE reference — every value in it is a
# requirement. Reformatting it would edit the source of truth we are
# meant to measure against. Same reason v3-plate is here.
docs/design/v5-momentum
.github/skills
.github/agents
.github/hooks
.codex
.impeccable
test-results
playwright-report

# The Expo app. Its SOURCE is formatted with everything else — same house
# style — so only the parts that are installed, generated or exported are
# skipped here.
mobile/node_modules
mobile/.expo-export
mobile/.expo
mobile/package-lock.json
# Generated from src/lib/tokens.ts by `npm run check:tokens -- --write`.
mobile/tailwind.tokens.js
mobile/expo-env.d.ts

# Generated by `expo prebuild`. Gitignored, and prettier walks it anyway:
# formatting the whole repo went from seconds to minutes once it existed.
mobile/ios/
mobile/android/
.expo-web/
